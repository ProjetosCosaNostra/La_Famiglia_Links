const qs=new URLSearchParams(location.search),product=qs.get('product')||'',campaign=qs.get('campaign')||'';const status=document.getElementById('gateStatus'),a=document.getElementById('gateLink');function ev(type,data={}){try{const k='blackgold.events.v1',x=JSON.parse(localStorage.getItem(k)||'[]');x.push({type,at:new Date().toISOString(),...data});localStorage.setItem(k,JSON.stringify(x.slice(-250)));}catch(e){}}async function run(){try{const r=await fetch('data/retailer-links.json',{cache:'no-store'});if(!r.ok)throw new Error('registry unavailable');const data=await r.json();const x=(data.links||[]).find(v=>v.product_id===product);const good=x&&x.public_state==='verified'&&/^https:\/\//.test(x.affiliate_url||'')&&x.last_verified_at;if(!good){status.textContent=(x&&x.reason)||'This retailer destination is not verified yet.';a.textContent='Retailer link locked';ev('retailer_gate_blocked',{product_id:product,campaign_id:campaign});return;}status.textContent='Verified retailer destination: '+x.retailer+'. Last verified '+x.last_verified_at+'.';a.href=x.affiliate_url;a.target='_blank';a.rel='noopener sponsored';a.classList.remove('locked');a.removeAttribute('aria-disabled');a.textContent='Continue to '+x.retailer;a.addEventListener('click',()=>ev('retailer_click',{product_id:product,campaign_id:campaign,retailer:x.retailer}));}catch(e){status.textContent='Retailer registry could not be verified. The link remains locked.';}}run();